{
  "name": "lftg-platform",
  "version": "13.0.0",
  "private": true,
  "description": "La Ferme Tropicale de Guyane - Plateforme de gestion",
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "db:generate": "pnpm --filter @lftg/core prisma:generate",
    "db:migrate": "pnpm --filter @lftg/core prisma:migrate",
    "db:seed": "pnpm --filter @lftg/core prisma:seed",
    "test": "pnpm --filter backend test && pnpm --filter backend test:e2e",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
